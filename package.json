{
  "name": "ppx_pipe_first",
  "version": "0.0.1",
  "description": "Pipe First: pipe first as a syntax transform",
  "esy": {
    "build": "dune build -p ppx_pipe_first"
  },
  "scripts": {
    "test": "esy dune runtest"
  },
  "dependencies": {
    "@opam/reason": "*",
    "@opam/dune": "*",
    "@opam/ocaml-migrate-parsetree": "*",
    "ocaml": "*"
  },
  "devDependencies": {
    "@opam/merlin": "*"
  }
}
